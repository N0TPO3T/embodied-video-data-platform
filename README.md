# Embodied Video Data Platform

一个面向具身智能视频数据生产的可点击平台框架，覆盖公开官网、统一演示登录、数采人员、团长和平台管理员四类界面。

当前版本使用浏览器内模拟数据，重点演示视频上传、AI 处理、质量复核、数据入库、每日结算和提现审核等核心业务状态；暂不连接真实账号、对象存储、AI 推理、数据库或支付接口。

## 已实现范围

- 公开官网：平台定位、公开指标、场景能力、生产流程、质量保障和体验入口。
- 数采人员：视频上传、我的数据、质检详情、收入提现、采集指南和个人资料。
- 团长：成员管理、团队数据、结算前复核、团队分析和团队收入。
- 平台管理员：全平台提交、AI 队列、质量复核、数据资产、用户团队、规则、结算、提现审核、公开配置和审计日志。
- 核心规则：角色数据隔离、质量系数、有效时长、预计收入、结算后不可修改和提现金额校验。

## 本地运行

环境要求：Node.js 22.13 或更高版本，pnpm。

```bash
cd web
pnpm install
pnpm dev
```

默认访问地址为 `http://localhost:3000`。

## 验证

```bash
cd web
pnpm test
pnpm typecheck
pnpm build
pnpm test:render
```

## 演示角色

- 数采人员：林晓雨
- 团长：周明远
- 平台管理员：陈屿

登录页无需密码即可选择身份；进入工作台后也可通过顶部的“演示角色”切换器快速体验不同权限范围。

## 工程结构

```text
web/
├── app/                  # vinext / Next 应用入口、全局样式和元数据
├── src/app/              # 客户端路由与角色边界
├── src/domain/           # 领域类型和纯业务计算
├── src/data/             # 演示数据与内存状态仓库
├── src/components/       # 表格、状态、复核抽屉等公共组件
└── src/features/         # 官网、登录、数采、团长和管理员页面
```

页面通过统一的演示数据仓库读取和更新状态。后续接入真实后端时，可以将该仓库替换为 API 适配层，同时保留现有页面结构和领域计算。
