# 当前项目待完善清单

- 盘点日期：2026-08-13
- 产品范围：本地网页版
- 当前核心方向：视频上传、AI 视频质检、人工复核、团队统计、积分记录

## 已经基本落地

- [x] 用户名密码登录、会话校验、退出、登录失败限制。
- [x] 管理员、团长、数采人员三类角色和页面权限边界。
- [x] 账号、团队、团长任命、成员管理和账号操作审计持久化。
- [x] MP4/MOV 分片上传到 MinIO，对象大小和 SHA-256 校验。
- [x] PostgreSQL 视频提交、媒体元数据、媒体问题片段、AI 提示词版本和 AI 质检结果。
- [x] RabbitMQ/outbox 驱动的媒体 Worker 和 AI 质检 Worker。
- [x] Qwen3.7 初检、条件复核、服务端规则复算和结果持久化。
- [x] 管理员人工复核最终分、问题区间、原因、复核人、复核时间和审计记录。
- [x] 前端主流程已从金额/提现口径改为积分/周期锁定口径。
- [x] 积分周期、积分明细快照、周期锁定 API 和锁定审计已持久化。
- [x] 锁定周期后的人工改分会生成积分调整流水，不覆盖原周期快照。
- [x] 积分周期支持按角色范围导出 CSV 明细，供线下结算核对。
- [x] 交付包、资产条目、创建审计、CSV 资产清单、短期签名资产下载链接、zip 和 tar 一键归档、可追踪异步归档准备任务和归档短期下载链接已持久化；资产页待交付资产、最近入库资产、包体积和交付包候选优先从后端计算。
- [x] 视频详情页支持按权限获取 HLS 多码率预览、低码率 MP4 回退和短期签名原视频回退。
- [x] 媒体处理会生成视频封面图和技术问题片段证据帧，详情页按权限展示短期签名图片。
- [x] 上传创建时按 SHA-256 拦截完全重复视频。
- [x] 数采人员刷新或更换设备后可看到账号内未完成上传，重新选择同名、同大小且 SHA-256 一致的原视频后恢复分片上传，并支持暂停、继续、取消和失败重试。
- [x] 数采人员“我的数据”和“质检结果”已接入服务端搜索、状态筛选、分页、本人权限范围和提交详情接口；视频详情继续按权限获取短期预览、HLS、封面和证据帧。
- [x] 媒体解析后会生成近似重复候选、相似度和候选视频引用；疑似重复在管理员确认前不进入积分锁定，管理员可在复核页解除误报并写入审计。
- [x] 管理员可查看后端 AI 队列快照、发布耗时统计、Worker 心跳、机器信息、当前任务、运行耗时、完成/失败数、平均/最近/最长处理耗时和运行过久告警，并对失败 AI 质检任务手动重跑、对超时运行任务重新排队，记录原因和审计。
- [x] 管理员质量复核页已接入后端复核候选范围，包含质量通过且未入周期的数据，并保留近似重复候选供管理员确认或解除。
- [x] 管理员首页已接入后端提交列表，视频提交、质量通过率、可交付资产和处理流水线状态优先从后端计算。
- [x] 管理员数据提交页已接入服务端搜索、状态筛选、分页、真实周期锁定状态和当前筛选结果 CSV 导出。
- [x] 管理员审计日志页已接入服务端关键词、操作人、动作、时间范围筛选、分页和当前筛选结果 CSV 导出。
- [x] 管理员质量规则版本、通过阈值和规则说明已持久化，发布新版本会写入审计。
- [x] 管理员标签体系已按版本快照持久化，编辑标签会生成新版本并写入审计。
- [x] 管理员积分规则版本、默认每分钟积分、质量系数档位和说明已持久化，发布新版本会写入审计。
- [x] 新 AI 质检任务会锁定提示词、质量规则和标签体系版本；新积分周期会锁定积分规则版本，历史结果保留快照依据。
- [x] 团长团队工作台、团队分析、成员详情指标和成员统计导出已接入后端团队提交列表，近 30 日指标、近 7 日趋势、场景分布、成员贡献、成员通过率、待关注和失败任务优先从后端计算；团队数据页已接入服务端搜索、状态筛选、分页和按当前筛选导出 CSV；团长只读质检结果页已接入服务端未锁定且质量通过的数据范围；数采积分明细和团队积分汇总已接入后端积分周期、周期明细和待锁定提交数据。
- [x] 公开官网指标、场景占比和公开配置已从真实质检通过数据生成脱敏快照，管理员发布会持久化并写入审计。
- [x] 通知中心和导航角标已接入真实运维状态摘要，按当前角色范围展示待处理、失败、复核和积分锁定提醒。
- [x] 上传前数据授权、隐私规范和敏感内容处理确认已前端阻断、后端校验并记录确认版本与时间。
- [x] 登录和敏感写接口已接入服务端频率限制，过期会话会自动清理，生产模式会拦截本地默认密钥和本地预设账号密码。
- [x] 敏感视频支持 AI 自动隔离和人工复核隔离/解除隔离，隔离后不进入普通待结算资产、交付包候选或公开官网统计，并写入审计。
- [x] 对象存储访问审计、默认保留周期、管理员强制删除同步和本地备份/恢复演练说明已落地。
- [x] CI、数据库迁移发布流程、容量/性能验收口径和告警阈值起点已落地。

## P0：仍需补齐的真实业务闭环

- [x] 当前列出的 P0 业务闭环已补齐；后续重点转为真实部署演练、长时间压测、模型效果调优和测试环境异步提示清理。

## 已明确不做

- [ ] 平台内真实提现、自动付款、在线支付和打款流程。
- [ ] 独立客服聊天、工单系统、采购商门户和在线合同。
- [ ] iPhone 专用录制 App、IMU、6DoF、深度和标定文件。
- [ ] 团长佣金、渠道差价、个人特殊单价和复杂财务角色。

## 当前完成度判断

项目已经从“可点击原型”推进到“账号、登录限流、过期会话清理、生产默认密码保护、上传、上传授权确认、暂停/继续/跨设备恢复上传、数采我的数据/质检结果/详情后端化、媒体处理、AI 质检、敏感视频隔离、近似重复候选与积分防重复、对象访问审计和删除治理、人工复核、管理员复核候选后端化、管理员首页后端化、积分周期锁定、锁定后调整流水、数采积分明细和团队积分汇总后端化、积分导出、交付包清单、交付候选和资产页后端化、短期资产下载链接、zip/tar 一键归档、可追踪归档准备任务、HLS 多码率预览、低码率 MP4 回退、封面和证据帧、队列快照、发布耗时、Worker 心跳、当前任务运行耗时、历史处理耗时和过久任务重新排队、管理端提交与审计日志服务端检索分页及 CSV 导出、团长工作台、团队分析、成员指标和团队数据服务端检索分页及 CSV 导出、团长质检结果后端只读视图、质量规则/标签体系/积分规则版本持久化和历史快照、公开官网脱敏快照、真实通知与导航角标、CI 和发布运维流程”的本地平台。下一步更适合做真实部署演练、长时间压测、近似重复模型效果调优，以及把少量测试环境异步提示清理干净。
